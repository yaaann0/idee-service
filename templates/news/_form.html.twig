{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.newsFile) }}
    {{ form_errors(form) }}
    <input class="btn btn-primary" type="submit" value="{{ submitText }}"/>
{{ form_end(form) }} 
