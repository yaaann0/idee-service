{% extends 'emails/base.html.twig' %}

{% block title %}{{ contact.subject }}{% endblock %}

{% block body %}
    
    <div id="main" class="">
        <p>Message de {{ contact.user.firstname | capitalize }} {{ contact.user.lastname | upper }} :</p>

        <div>
            {{ contact.content }}        
        </div>
    </div>

{% endblock %}
